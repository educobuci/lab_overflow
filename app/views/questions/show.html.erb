<h2><%= @question.title %></h2>

<p>
  <%= @question.text %>
</p>

<%= form_for [@question, @question.answers.build] do |a|%>
  <%= a.text_area :text %>
  <%= a.submit :value => 'Post your Answer' %>
<% end %>

<%= link_to 'Back', "/" %>
